{% set name = "batoid" %}
{% set version = "0.8.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: a95ce55d65d1955579f2765bce576e399d28e73b59b45f33f24db40fb8a2c96a

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  skip: true  # [win]
  skip: true  # [py==38]

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]
    - pybind11                               # [build_platform != target_platform]
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
    - cmake
    - make
    - llvm-openmp  # [osx]
    - libgomp      # [linux]
  host:
    - python
    - pip
    - setuptools
    - pybind11
    - numpy
    - pytest-runner
  run:
    - python
    - pyyaml
    - scipy
    - galsim
    - matplotlib-base
    - astropy-base

test:
  imports:
    - batoid
  requires:
    - pip
  # commands:
  #   # fails due to galsim requirement on setuptools
  #   - pip check

about:
  home: https://github.com/jmeyers314/batoid
  summary: 'c++ backed python optical raytracer'
  license: BSD-2-Clause
  license_family: BSD
  license_file: LICENSE

extra:
  recipe-maintainers:
    - beckermr
    - jmeyers314
